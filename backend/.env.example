# Python ML service (backend) environment.
# Copy to .env or set these in your host's dashboard.

# Platform port and host.
PORT=5001
HOST=0.0.0.0

# Set to 1 to force deterministic mock mode (no PyTorch needed).
# Leave unset for real AI.
DERMA_MOCK=
