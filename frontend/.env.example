# Frontend (React) environment.
# In production, point this at your deployed Spring Boot gateway URL.
# The app will call <base>/api automatically.
VITE_API_BASE_URL=https://your-gateway.onrender.com
