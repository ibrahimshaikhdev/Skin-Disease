# Frontend (React) environment
# In production, point this at your deployed Spring Boot gateway BASE url.
# Do NOT include /api at the end — the app appends it automatically.
# Example: https://your-gateway.onrender.com
VITE_API_BASE_URL=https://your-gateway.onrender.com
