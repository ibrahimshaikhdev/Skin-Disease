name: Keep Render App Awake

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Visit site
        run: |
          curl -L -s -o /dev/null -w "%{http_code}\n" \
          https://resume-iq-itt9.onrender.com/
